---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<button
  class={`bg-gradient1 hover:bg-gradient1-50 px-9 py-3.5 xl:py-5 rounded-full text-body-s xl:text-base font-bold text-white ${className}`}
  ><slot /></button
>
