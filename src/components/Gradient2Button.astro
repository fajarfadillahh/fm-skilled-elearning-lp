---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<button
  class={`bg-gradient2 hover:bg-gradient2-50 px-6 py-2.5 rounded-full text-body-s font-bold text-white ${className}`}
  ><slot /></button
>
